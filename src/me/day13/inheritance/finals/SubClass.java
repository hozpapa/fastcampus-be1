package me.day13.inheritance.finals;

// 상속받으려고 하는 클래스가 final 로 정의가 되어있으면 상속 불가
public class SubClass /*extends SuperClass*/ {
    // 상속을 불가능하게 하는 이유 ?
    // 자바에서 클라이언트 사용을 위해 API로 제공하는 클래스 (String class)
}

